/**
 * @typedef {Object} ApproachingBus
 * @property {string} hatKodu
 * @property {string} hatAdi
 * @property {number} kalanSureDk
 * @property {number} dolulukOrani
 * @property {boolean} engelliErisimi
 * @property {number} beklenenYolcu — ML (veya yedek) tahmini yolcu sayısı
 * @property {boolean} mlTahmin — true ise XGBoost cevabı kullanıldı
 * @property {number} aracKapasitesi — doluluk oranı paydası
 */

export {};
